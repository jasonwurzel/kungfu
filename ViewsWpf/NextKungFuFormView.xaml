<UserControl x:Class="ViewsWpf.NextKungFuFormView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ViewsWpf"
             xmlns:viewModels="clr-namespace:ViewModels;assembly=ViewModels"
             xmlns:interfaces="clr-namespace:Interfaces;assembly=Interfaces"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance interfaces:INextKungFuFormViewModel}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBlock Text="Next Form"/>
        <TextBlock Grid.Row="1" Text="{Binding NextFormText}"/>
        <Button Grid.Row="2" Content="Next" Command="{Binding GetNextForm}"/>
    </Grid>
</UserControl>
